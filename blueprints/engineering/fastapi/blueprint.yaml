name: engineering-fastapi
version: 1.0.0
extends: engineering
description: FastAPI + Alembic + router-doc sync rules (AI Wealth Health Check lineage).

includes:
  rules:
    - fastapi-guidelines.mdc
    - alembic-cli-migrations-only.mdc
    - router-endpoint-readme-sync.mdc
    - key-principles.mdc

notes: |
  key-principles.mdc is project-lineage content. Consumers should override with
  key-principles.local.mdc for their own stack identity.
  Treat FastAPI rules as optional; delete or disable if the target repo is not FastAPI.
