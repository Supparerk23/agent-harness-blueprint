# Example generated install state for a consuming project.
# In a real repo this file is written by `scripts/agent install`.
# source is a portable repository identity (not a machine-local path).
source: agent-harness-blueprint
version: 1.0.0
blueprint: engineering
overlay: gitlab
runtimes:
  - cursor
  - claude
installed_at_utc: 2026-07-29T00:00:00Z
conflict_policy: preserve-local
